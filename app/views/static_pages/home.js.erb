$('#feed_container').append('<%= j render partial: "posts/post", collection: @feed, as: :post  %>');
<% if @feed.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate(@feed) %>')
<% else %>
    $('.pagination').remove();
<% end %>