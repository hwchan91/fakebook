Request sent
<%= form_for(current_user.friendships.find_by(friend_id: user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Undo", class: "btn btn-secondary" %>
<% end %>
