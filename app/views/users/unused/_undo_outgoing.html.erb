<%= form_for(current_user.active_requests.find_by(receiver_id: user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <div><%= hidden_field_tag :page, "outgoing" %></div>
  <%= f.submit "Undo", class: "btn btn-secondary" %>
<% end %>
