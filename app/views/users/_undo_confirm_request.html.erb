Request accepted
<%= form_for(current_user.friendships.find_by(friend_id: user.id), remote: true) do |f| %>
  <div><%= hidden_field_tag :requestor_id, user.id %></div>
  <%= f.submit "Undo", class: "btn btn-secondary" %>
<% end %>
