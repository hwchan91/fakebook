<% if user.avatar.present? %>
  <%= image_tag user.avatar.url(:thumb), class: "avatar_thumb" %>
<% elsif user.fb_avatar %>
   <%= image_tag user.fb_avatar, class: "avatar_thumb" %>
<% else %>
  <%= image_tag user.avatar.url(:thumb), class: "avatar_thumb" %>
<% end %>
